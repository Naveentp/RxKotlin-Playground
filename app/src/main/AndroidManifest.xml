<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="neo.rxkotlin.playground">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="neo.rxkotlin.playground.LandingPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="neo.rxkotlin.playground.OperatorsActivity" />
        <activity android:name="neo.rxkotlin.playground.operators.SimpleExampleActivity" />
        <activity android:name="neo.rxkotlin.playground.operators.MapExampleActivity" />
        <activity android:name=".operators.ZipExampleActivity" />
        <activity android:name=".operators.FilterExampleActivity" />
        <activity android:name=".operators.TakeExampleActivity" />
        <activity android:name=".operators.ReduceExampleActivity" />
        <activity android:name=".operators.SkipExampleActivity"/>
        <activity android:name=".operators.FlatMapExampleActivity"/>
    </application>

</manifest>